<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.world.playstay.user.dao.UserMapper">
    <insert id="insert" parameterType="com.world.playstay.user.entity.User">
        INSERT INTO USER (name, email)
        VALUES (#{name}, #{email})
    </insert>

    <delete id="delete" parameterType="Long">
        DELETE FROM USER WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="Long" resultType="com.world.playstay.user.entity.User">
        SELECT id, name, email, created_at
        FROM USER
        WHERE id = #{id}
    </select>
</mapper>